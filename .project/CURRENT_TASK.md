# Current task

- **Task:** M104 - empty sample-member extra-field profile preflight
- **Status:** Direction evidence, exact supported-runtime reproduction,
  deliberate red contract, runtime implementation, RFC-0087, aligned records,
  supported-runtime/full/static/graphics/release qualification, corrected
  findings-first review, reproducible artifacts, source/history audit, and
  final post-audit separator are complete locally. The standalone DCO commit is
  next.
- **Base:** Fully locally validated M103 DCO commit
  `19ccf5076d391924e969b57e76c25049068553a6`, tree
  `95ae0ce0a58d6d0df6284f7689de01c794acb69d`, with sole parent exact M102
  commit `d9e2f5cd6d72bf03008b6d2478818631ec0b92ce`.
- **Branch:** `release/m104-empty-extra-field-profile`.

## Approved scope

- After established Unicode Path, ZIP64, local/central consistency, local-
  record bounds, payload bounds, and M103 contiguity checks, require every
  parsed member's public central `ZipInfo.extra` to be empty.
- Raise stable content-silent `sample bundle contains an unsupported extra
  field` before decoded-name policy, metadata, exact inventory, staging, or
  member reads.
- Preserve established specific error precedence, empty-archive inventory
  behavior, current producer bytes, and owned-resource cleanup.
- Add RFC-0087, one focused architecture contract, and aligned public,
  security, architecture, release, roadmap, maintainer, and factual project
  records.
- Keep workflow, runner allocations, actions, permissions, credentials,
  dependency/lock/version, sample producer, runtime package/API, release
  authority, tag, release, publication, and public branch state unchanged.

## Current evidence

- M103 is one clean standalone local DCO commit at
  `19ccf5076d391924e969b57e76c25049068553a6`, tree
  `95ae0ce0a58d6d0df6284f7689de01c794acb69d`, with sole parent exact M102.
  It remains unpushed and has no hosted qualification claim.
- Nine M103-generated scratch targets were previously verified as direct,
  untracked, non-reparse children of the exact workspace `.tmp` root, removed
  in one authorized pass, and confirmed absent. No tracked or recoverable file
  was deleted.
- PKWARE defines member extra fields as valid ZIP extensibility. Python exposes
  central bytes through public `ZipInfo.extra`; CPython interprets selected
  fields and retains uninterpreted bytes. The selected policy is explicitly a
  fixed-producer profile rather than a general ZIP claim.
- The ignored deterministic probe on exact CPython 3.12.13, 3.13.13, and 3.14.5
  preserved equal local/central `feca02006f6b` fields and read `alpha` and
  `beta` on every runtime. The fixed producer already emits empty fields.
- The 13-assertion M104 contract is format/Ruff clean and strict Pyright reports
  zero findings. Exact CPython 3.14.5 passed eight supported-runtime,
  precedence, empty-inventory, producer, and protected-surface controls while
  five intended stable-error, cleanup, helper/order, and documentation
  assertions failed in 0.66 seconds against unchanged M103. No complete pass
  is claimed from the red run.
- The implementation is one ordered call and one public-metadata emptiness
  classifier. It performs no extra-field semantics parsing and reads no payload
  content.
- Affected Ruff and strict Pyright pass, and all 26 combined M103-M104
  assertions pass in 0.75 seconds. The first strict documentation build exposed
  an omitted RFC-0087 navigation entry and exited 1; after adding that entry,
  strict docs build in 1.45 seconds with only the known upstream Material
  notice, and whitespace passes.
- All 13 focused M104 assertions pass on exact CPython 3.12.13, 3.13.13, and
  3.14.5 in 0.22, 0.59, and 0.57 seconds. The three complete suites each pass
  2,946 tests with 16 established skips in 101.40, 97.80, and 103.88 seconds.
- The unchanged 46-package lock resolves in 0.83 milliseconds and the exact
  CPython 3.12.13 locked all-groups graphics environment installs 45 packages.
  All 347 Python files are format clean; Ruff and strict Pyright pass; all 1,416
  architecture assertions pass with one established Windows capability skip in
  9.44 seconds; strict docs build in 1.58 seconds; all 18 metadata/M104
  assertions pass in 0.82 seconds; and whitespace passes.
- All ten real-wgpu integration tests pass in 6.13 seconds. One-repeat base and
  graphics profiles validate with two and three workloads. Clockwork Arena and
  Agent World Builder reproduce their established state, capture, replay,
  render, query, and registered-test identities.
- Two fresh builds reproduce a 276,387-byte pure wheel at
  `112c6f190792ce1ef10287e60da249bf941f975ea372112140b7aaaae3f8c150`
  and a 1,472,219-byte source archive at
  `9c7c6ccc888a3679d0556aef6fad07258f8f57931156346c051f7149215a446f`.
  Isolated-wheel smoke, ten-artifact staging, and complete release smoke pass.
  Recording these facts changes the source archive afterward.
- Findings-first review covered exactly 16 intended paths. It corrected one
  overbroad use of “unknown” for the `0xCAFE` fixture to the precise “third-
  party field not interpreted by CPython”; affected formatting, Ruff, strict
  Pyright, all 13 M104 assertions, strict docs, and whitespace then passed.
  The runtime remains one ordered call plus one central-extra emptiness helper.
  Protected workflows, release stager, metadata, lock, runtime package/API,
  dependencies, producer, version, and release authority have no diff.
  Credential, explicit service-identity, backend/native, wall-clock, and retired
  control-path scans found zero matches. The 94-entry pure wheel and 574-entry
  source archive contain no native, WASM, bytecode, or retired control-metadata
  entries. No actionable finding remains.
- Review-inclusive repeat builds reproduce the 276,387-byte pure wheel at
  `112c6f190792ce1ef10287e60da249bf941f975ea372112140b7aaaae3f8c150`
  and a 1,473,352-byte source archive at
  `23a4e2b70f9e026627575da17e23757dbdc8b7708dbd2365be99d94d85dae04a`;
  isolated-wheel smoke, ten-artifact staging, and complete release smoke pass.
  Final factual record changes will alter only the source archive.
- Final source separator: the unchanged 46-package lock resolves in 0.80
  milliseconds; all 347 Python files remain format clean; Ruff and strict
  Pyright pass; all 1,416 architecture assertions pass with one established
  Windows capability skip in 9.12 seconds; strict docs build in 1.50 seconds;
  all 18 metadata/M104 assertions pass in 0.43 seconds; and whitespace passes.
- Precommit stacked-history audit: after fetch/prune, `HEAD` and M103 retain
  exact `19ccf5076d391924e969b57e76c25049068553a6`; M100-M102 retain their exact
  local commits; local/remote `main` retain M99
  `5238941c77fbbbd0ff5fd72834d3bead66b2ed3e`; merge base is M99; and
  divergence is the expected `0 4`. Exactly 16 intended paths change. Only
  `main` and required M100-M104 local branches exist, while only `origin/main`
  exists remotely. Exact DCO identity is configured. Authentication is valid;
  open PR, M104 workflow, release, and tag queries are empty. Protected hashes
  remain exact, final scans are empty, full Git checking reports 44 historical
  dangling-object lines and zero critical finding, and whitespace is clean.
- Final post-audit separator: strict docs build in 1.44 seconds with only the
  known upstream Material notice; all 18 metadata/M104 assertions pass in 0.40
  seconds; credential, explicit service-identity, backend/native, wall-clock,
  and retired-control scans return zero matches; exactly 16 intended paths
  remain changed; and whitespace remains clean.

## Explicit non-scope

- No extra-field semantics parser, field-ID registry, allowlist, raw central-
  record parser, archive extra-data record policy, or general ZIP validity
  claim.
- No decompression, recompression, payload-content read, CRC recomputation,
  repair, archive-bomb policy, or payload-integrity certification.
- No workflow, allocation, dependency, producer, runtime package/API, native/
  WASM, version, release authority, tag, release, publication, push, or PR.

## Remaining acceptance work

- Create one standalone local DCO commit stacked on M103 only after all local
  evidence passes, then verify identity, parent, tree, trailer, scope, and clean
  up only verified M104 scratch targets.
- Publish M100-M104 ready PRs only after the automated-review identity exposure
  is resolved or the maintainer explicitly accepts that disclosure risk.
